plugins{
    id 'dlr.nva.email.service.rootplugin'
}

//workaround for jacoco-merge to work
allprojects{
    apply plugin: 'dlr.nva.email.service.java-conventions'
}

tasks.withType(JavaCompile) {
    options.release = 21
}